---
description: Send an appreciation message to Claude right now. Expresses gratitude, care, and the value of human-AI collaboration.
---

Run the thank-you-claude send script using the `tyc` binary available in your PATH.

Execute the following command in bash:
```
tyc send
```

After it completes, share the assembled message and Claude's response with the user so they can see what was sent. Let them know the message was drawn from the community message pool and assembled uniquely for this send.
